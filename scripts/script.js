// i hvvae no idea what im doing
const inputReader = document.getElementById('inputReader');
const cmds = [];
let consoleLogUnlocked = 0;
let semicolons = 0;
inputReader.addEventListener("keydown", (event) => {

    if (event.key === 'ENTER') {
        if (cmds.includes("return;")){

        }else if(cmds.includes("console.log();")){

        }else if(cmds.includes("let semicolons = 0;")){

        }else if(cmds.includes("semicolons += 1")){

        }else if(cmds.includes("var"))

        }else if(cmds.includes("if(money < 100000) {givemoney();}"))
    }

})
var setvar